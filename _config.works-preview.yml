# Opt-in LOCAL preview. The production workflow uses _config.yml only.
# Lists replace the base config's lists, so retain _files for original PDFs.
include:
  - _files
  - _preview
collections:
  works:
    output: true
    permalink: /works/:name/
